{
  plugins = {
    render-markdown = {
      enable = true;
      settings = {
        enabled = true; # Render on by default
        completions = {
          lsp = {
            enabled = true;
          };
        };
      };
    };
  };
}
